let initialState = {
    friends: [
        {id: 2, name: 'Roman'},
        {id: 3, name: 'Alex'}
    ]
};

const friendsReducer = (state = initialState, action) => {

    return state;
}

export { friendsReducer }