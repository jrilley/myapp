#: Чи показувати стару анкету заявок («📝 Нова заявка», «📋 Мої заявки»,
#: «🗂 Усі заявки», «📊 Статистика»).
#:
#: Вимкнено на прохання замовника: робочою одиницею став рейс, а заявки
#: наразі не потрібні. Саме прапорець, а не закоментований код: код лишається
#: живим — компілюється, лінтується й покритий тестами, — тож повернути його
#: означає змінити один рядок, а не відновлювати те, що встигло зіпсуватись.
#:
#: Що вимкнення НЕ чіпає: таблицю applications, REST API та сайт. Сім наявних
#: заявок лишаються на місці й далі видно на сайті — прибрана лише робота з
#: ними в боті.
SHOW_APPLICATIONS = False

CATEGORIES: tuple[str, ...] = (
    "Технічна підтримка",
    "Співпраця",
    "Скарга",
    "Пропозиція",
    "Інше",
)

MAX_FULL_NAME = 200
MAX_CONTACT = 200
MAX_DESCRIPTION = 1000
MIN_DESCRIPTION = 10

# Реєстрація співробітника
MAX_FULLNAME = 200
MIN_PHONE = 6
MAX_PHONE = 32
MAX_COMPANY_NAME = 200
MAX_TAX_ID = 32
MAX_ADDRESS = 300
MAX_POSITION = 64

# Транспорт
MAX_VEHICLE_MAKE_MODEL = 120
MIN_LICENSE_PLATE = 3
MAX_LICENSE_PLATE = 16

# Рейси
MAX_TTN = 64
#: Спільна межа для коротких текстових полів рейсу: марка тягача, причіп,
#: тип причепа, культура, ПІБ водія.
MAX_TRIP_TEXT = 120
#: Кг. Верхня межа свідомо із запасом — вона ловить описку на зайвий нуль,
#: а не встановлює норматив завантаження.
MAX_TRIP_MASS = 200_000

#: Підказки для кнопок, а не довідник: у колонці лишається текст, і будь-що
#: поза переліком можна ввести руками. Але доки культуру набирали щоразу,
#: «Пшениця», «пшениця» й «пшеніца» ставали трьома різними культурами, і
#: жоден звіт їх не склав би.
GRAIN_TYPES: tuple[str, ...] = (
    "Пшениця",
    "Кукурудза",
    "Соняшник",
    "Ріпак",
    "Ячмінь",
    "Соя",
)

#: Види причепів живуть у models.TRAILER_TYPES — там, де й довідник
#: транспорту, з якого вони виводяться.

#: Формати, у яких рейс зберігає дату й час. Валідація вводу спирається
#: на них же, щоб у колонці не опинилось двох різних написань.
DATE_FORMAT = "%Y-%m-%d"
DATETIME_FORMAT = "%Y-%m-%d %H:%M"


def parse_chat_id(raw: str) -> int | None:
    """Id чату з тексту, або None, якщо це не схоже на id.

    Живе тут, бо чат вводиться у двох місцях: при заведенні компанії та при
    редагуванні її картки, — і правило має бути одне.

    int() з try/except не годиться: він прийме «+5», «  7  » і «1_0».
    """
    value = raw.strip()
    negative = value.startswith("-")
    digits = value[1:] if negative else value
    if not digits.isdigit() or not 1 <= len(digits) <= 20:
        return None
    return -int(digits) if negative else int(digits)
